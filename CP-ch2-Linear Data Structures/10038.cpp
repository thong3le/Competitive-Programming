#include <cstdio>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;

int main(){
	int n, v;
	vector<int> array;
	while (scanf("%d", &n) != EOF) {
		while (n--) {
			scanf("%d", &v);
			array.push_back(v);
		}

	}
}